# Laufzeit-Abhängigkeiten für exceltool.py
# Installation:  pip install -r requirements.txt
pandas>=1.3
openpyxl>=3.0
xlrd>=2.0        # liest .xls (BIFF); 2.x unterstützt bewusst kein .xlsx mehr
# tkinter ist Teil der Standardbibliothek (unter Linux ggf. python3-tk installieren)
